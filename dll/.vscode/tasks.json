{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build iflycli_dll",
            "type": "shell",
            "command": "cmake --build build",
            "group": "build",
            "problemMatcher": ["$gcc", "$msCompile"]
        }
    ]
}
